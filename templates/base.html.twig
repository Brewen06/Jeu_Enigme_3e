<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Accueil{% endblock %}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    {% block stylesheets %}{% endblock %}
    {% block javascripts %}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}
</head>
<body class="min-h-screen flex items-center justify-center bg-gray-100">
    {% block body %}
        <div class="bg-white/90 backdrop-blur-md p-10 rounded-2xl shadow-xl max-w-xl w-full text-center">
            
            <h1 class="text-4xl font-extrabold bg-clip-text text-gray-700 mb-6">
                Bienvenue sur le jeu d'énigmes !
            </h1>
            
            <a href="{{ path('creer_equipe') }}" 
               class="inline-block px-6 py-3 text-lg font-semibold text-red-500 rounded-lg 
                      hover:scale-105 transform transition duration-300 border-2 border-red-500 
                      hover:bg-red-500 hover:text-white">
                Créez votre équipe.
            </a>

            <a href="{{ path('app_jeu') }}"
                class="inline-block px-6 py-3 text-lg font-semibold text-blue-500 rounded-lg 
                       hover:scale-105 transform transition duration-300 border-2 border-blue-500 
                       hover:bg-blue-500 hover:text-white mt-4">
                Démarrer le jeu.
            </a>
        </div>
    {% endblock %}
</body>
</html>
