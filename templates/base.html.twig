<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Accueil{% endblock %}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    {% block stylesheets %}{% endblock %}
    {% block javascripts %}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}
</head>
<body class="min-h-screen flex items-center justify-center bg-gray-100">
    {% block body %}
        <div class="bg-white/90 backdrop-blur-md p-10 rounded-2xl shadow-xl max-w-xl w-full text-center">
            
            <h1 class="text-4xl font-extrabold text-transparent bg-clip-text text-red-500 mb-6 drop-shadow-md">
                Bienvenue sur le jeu d'énigmes !
            </h1>
            
            <a href="{{ path('creer_equipe') }}" 
               class="inline-block px-6 py-3 text-lg font-semibold text-white bg-gradient-to-r from-blue-500 to-purple-600 rounded-lg 
                      hover:scale-105 transform transition duration-300">
                Créez votre équipe.
            </a>
        </div>
    {% endblock %}
</body>
</html>
