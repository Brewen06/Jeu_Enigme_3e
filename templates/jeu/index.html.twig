{% extends 'base.html.twig' %}

{% block title %}Démarrage{% endblock %}

{% block body %}

<div class="max-w-xl mx-auto mt-12 p-8 bg-white rounded-lg shadow-lg">
    <h1 class="text-3xl font-bold text-blue-600 mb-4 flex items-center gap-2">
        Bonjour {{ equipe_nom }}!
    </h1>

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <a href="{{ path('app_choisir_enigme'){{id}} }}" class="w-full mt-4 py-2 px-4 bg-gradient-to-r from-blue-500 to-red-500 text-white font-semibold rounded-lg shadow-md hover:from-blue-600 hover:to-red-600 transition-colors duration-200 inline-block text-center">
        Démarrer
        <i class="fas fa-play ml-2"></i>
    </a>
</div>
{% endblock %}